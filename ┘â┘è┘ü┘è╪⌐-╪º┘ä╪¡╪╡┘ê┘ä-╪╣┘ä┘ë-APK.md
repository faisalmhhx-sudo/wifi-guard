# كيف تحصل على ملف APK لتثبيته على جوالك؟ 📱

التطبيق (كود Flutter) جاهز بالكامل. يبقى فقط "بناؤه" لملف APK.
اختر أسهل طريقة تناسبك — كلها بدون تثبيت أي شيء على جوالك:

---

## الطريقة ١: FlutLab.io (الأسهل — من المتصفّح مباشرة) ⭐

موقع يبني تطبيقات Flutter من المتصفّح بدون أي برامج.

1. ادخل على https://flutlab.io وسجّل دخول (مجاني).
2. أنشئ مشروع جديد، ثم ارفع ملفات المشروع (من مجلد `wifi_guard`).
3. من قائمة **Build** اختر **Build APK**.
4. حمّل ملف الـ APK وانقله لجوالك وثبّته.

> على جوالك: فعّل «تثبيت من مصادر غير معروفة» عند الطلب.

---

## الطريقة ٢: GitHub Actions (تلقائي بالكامل) 🤖

المشروع فيه ملف بناء جاهز `‎.github/workflows/build-apk.yml‎` يبني الـ APK تلقائياً.

1. أنشئ حساب على https://github.com (مجاني).
2. أنشئ مستودعاً (Repository) جديداً وارفع فيه مجلد `wifi_guard`.
3. افتح تبويب **Actions** — سيبدأ البناء تلقائياً (٥–٧ دقائق).
4. بعد اكتمال البناء، ادخل على آخر تشغيل وحمّل الملف من قسم **Artifacts** باسم `wifi-guard-apk`.

---

## الطريقة ٣: Codemagic (خدمة بناء متخصّصة) 🔧

1. ادخل https://codemagic.io وسجّل دخول بحساب GitHub.
2. اربط مستودع المشروع.
3. اختر Flutter → **Build** → حمّل الـ APK.

---

## الطريقة ٤: على حاسبك (لو عندك كمبيوتر) 💻

إذا عندك كمبيوتر وتقدر تثبّت [Flutter](https://docs.flutter.dev/get-started/install):

```bash
cd wifi_guard
flutter create . --platforms=android
flutter pub get
flutter build apk --release
```

ملف الـ APK بيطلع في:
`build/app/outputs/flutter-apk/app-release.apk`
انقله لجوالك وثبّته.

---

## بعد التثبيت
- افتح التطبيق وأعطِه صلاحية **الموقع** و**الإشعارات** (مطلوبة للمسح والتنبيهات).
- اضغط **فحص الشبكة** ليكتشف الأجهزة المتصلة.
- فعّل **المراقبة في الخلفية** من الإعدادات ليصلك تنبيه عند دخول أي جهاز جديد.

> نصيحة: أنصح بالطريقة ١ (FlutLab) إن كنت تريد الأسرع بدون حساب GitHub،
> أو الطريقة ٢ إن أردت بناءً تلقائياً يتحدّث مع كل تعديل.
